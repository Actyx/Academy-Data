export const freezeBatch = (batchId: string) => {
  alert(`Implementation missing:\n${batchId} is not going to be frozen.`)

  // Practice exercise:
  // Implement a freeze methode into the Batch-Material-Twin. 
  // Operators may freeze a batch if they encounter quality problems
  // regarding the raw materials in the batch. 
  // Materials from a 'frozen'  batch may not be consumed. 

  //   1. add frozen event
  //   2. add emitter
  //   3. add frozen State
  //   4. add new behaviour to onEvent Function
  //   5. add new behaviour to machine-connector
  //   6. update worker-ui to show frozen notification
}
