import { AppManifest } from "@actyx/pond"

export const manifest: AppManifest = {
  appId: 'com.example.node.app',
  displayName: 'machine-connector-v2',
  version: '0.0.1'
}

export default manifest
